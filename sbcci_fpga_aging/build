#!/bin/bash

# Use the first argument as job count, default to 12 if not provided
JOBS=${1:-12}

echo "==========================================="
echo "   Building FPGA Project with $JOBS jobs"
echo "==========================================="

# Run Vivado in batch mode
vivado -mode batch -source build12jobs.tcl -tclargs $JOBS
